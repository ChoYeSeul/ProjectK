<script>
    const sizeChart = document.getElementById("ProductSizeChart");
    if (sizeChart) {
      const sizeChartContent = sizeChart.innerHTML;
      const sizeTab = document.getElementById("ProductTab1");
      if (sizeTab) {
        sizeTab.innerHTML = sizeChartContent;
      }
    }
  </script>